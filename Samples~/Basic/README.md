# Basic Sample

This V1 clean export intentionally ships without concrete actions or runtime playback.

Create project-specific `TimelineAction` subclasses and a project-owned reader/playback layer to use timelines at runtime.
